<winuiex:WindowEx
    x:Class="TopToolbar.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:winuiex="using:WinUIEx"
    xmlns:local="using:TopToolbar.Models"
    xmlns:vm="using:TopToolbar.ViewModels"
    xmlns:workspaces="using:TopToolbar.Services.Workspaces"
    xmlns:tkcontrols="using:CommunityToolkit.WinUI.Controls"
    xmlns:conv="using:TopToolbar.Converters"
    mc:Ignorable="d"
    Width="1080"
    Height="720"
    MinWidth="960"
    MinHeight="640"
    Title="Dev Grid Settings">
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.Resources>
            <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <conv:GroupIdToRenameUiConverter x:Key="GroupIdToRenameUiConverter"/>
            <!-- Global smaller base text size override (excluding explicit sizes) -->
            <Style TargetType="TextBlock"
                    x:Key="BaseTextStyle">
                <Setter Property="FontSize"
                        Value="12"/>
            </Style>
            <!-- Spacing tokens -->
            <x:Double x:Key="SpacingXS">4</x:Double>
            <x:Double x:Key="SpacingS">8</x:Double>
            <x:Double x:Key="SpacingM">12</x:Double>
            <x:Double x:Key="SpacingL">20</x:Double>
            <!-- Standardized width for single-line text inputs to ensure consistent visual rhythm -->
            <x:Double x:Key="StandardInputWidth">360</x:Double>
            <LinearGradientBrush x:Key="SettingsAccentGradientBrush" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FF4F8BFF" Offset="0"/>
                <GradientStop Color="#FF7C4DFF" Offset="1"/>
            </LinearGradientBrush>
            <SolidColorBrush x:Key="SettingsQuietCardBrush" Color="#10FFFFFF"/>
            <SolidColorBrush x:Key="SettingsQuietStrokeBrush" Color="#24FFFFFF"/>
            <Style x:Key="SettingsPageTitleStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="26"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="TextWrapping" Value="NoWrap"/>
            </Style>
            <Style x:Key="SettingsPageSubtitleStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Opacity" Value="0.68"/>
                <Setter Property="TextWrapping" Value="WrapWholeWords"/>
            </Style>
            <Style x:Key="SettingsSectionLabelStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Opacity" Value="0.62"/>
                <Setter Property="CharacterSpacing" Value="48"/>
            </Style>
            <Style x:Key="SettingsPillButtonStyle" TargetType="Button">
                <Setter Property="CornerRadius" Value="9"/>
                <Setter Property="Padding" Value="14,7"/>
                <Setter Property="MinHeight" Value="34"/>
            </Style>
            <!-- Reusable style for standard single-line TextBox fields -->
            <Style x:Key="StandardTextBoxStyle"
                    TargetType="TextBox">
                <Setter Property="HorizontalAlignment"
                        Value="Left"/>
                <Setter Property="Width"
                        Value="{StaticResource StandardInputWidth}"/>
                <Setter Property="CornerRadius"
                        Value="8"/>
            </Style>


            <!-- Small square icon button style (fallback if not already provided elsewhere) -->
            <Style x:Key="ButtonStyleSmallIcon"
                    TargetType="Button">
                <Setter Property="Background"
                        Value="Transparent"/>
                <Setter Property="BorderBrush"
                        Value="Transparent"/>
                <Setter Property="BorderThickness"
                        Value="0"/>
                <Setter Property="CornerRadius"
                        Value="6"/>
                <Setter Property="Padding"
                        Value="4"/>
                <Setter Property="HorizontalAlignment"
                        Value="Center"/>
                <Setter Property="VerticalAlignment"
                        Value="Center"/>
                <Setter Property="Width"
                        Value="28"/>
                <Setter Property="Height"
                        Value="28"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid x:Name="Root"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="{TemplateBinding CornerRadius}">
                                <ContentPresenter HorizontalAlignment="Center"
                                        VerticalAlignment="Center"/>
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background"
                                                        Value="#14000000"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background"
                                                        Value="#22000000"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Opacity"
                                                        Value="0.5"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- List item style (shared) -->
            <Style x:Key="ToolbarListItemStyle"
                    TargetType="ListViewItem">
                <Setter Property="Margin"
                        Value="0,4,0,0"/>
                <Setter Property="Padding"
                        Value="10,8"/>
                <Setter Property="HorizontalContentAlignment"
                        Value="Stretch"/>
                <!-- Base (normal) background kept subtle neutral -->
                <Setter Property="Background"
                        Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListViewItem">
                            <Grid x:Name="RootGrid"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="8">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <!-- Main content presenter -->
                                <ContentPresenter
                                    x:Name="ContentPresenter"
                                    ContentTransitions="{TemplateBinding ContentTransitions}"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    Margin="{TemplateBinding Padding}"
                                    VerticalAlignment="Center"/>
                                <!-- Overlay accent stripe -->
                                <!-- Accent stripe (Strategy A): visibility bound directly to IsSelected; older opacity/state logic removed -->
                                <Border x:Name="AccentStripe"
                                        Width="3"
                                        Margin="0,2,0,2"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Stretch"
                                        CornerRadius="2"
                                        Background="#FF1E5AA7"
                                        Visibility="{Binding IsSelected, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVisibilityConverter}}"
                                        x:SuppressXamlTrimWarnings="True"
                                        IsHitTestVisible="False"/>
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background"
                                                        Value="{ThemeResource SubtleFillColorSecondaryBrush}"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background"
                                                        Value="{ThemeResource SubtleFillColorTertiaryBrush}"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Opacity"
                                                        Value="0.45"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                    <VisualStateGroup x:Name="SelectionStates">
                                        <VisualState x:Name="Unselected"/>
                                        <VisualState x:Name="Selected">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background"
                                                        Value="{ThemeResource CardBackgroundFillColorSecondaryBrush}"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="SelectedPointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background"
                                                        Value="{ThemeResource SubtleFillColorSecondaryBrush}"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="SelectedPressed">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background"
                                                        Value="{ThemeResource SubtleFillColorTertiaryBrush}"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="SelectedUnfocused">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background"
                                                        Value="{ThemeResource CardBackgroundFillColorSecondaryBrush}"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="SelectedDisabled">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Background"
                                                        Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
                                                <Setter Target="RootGrid.Opacity"
                                                        Value="0.5"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar"
                Height="56"
                Background="Transparent"
                Padding="18,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="96"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Center"
                    Spacing="12">
                <Border Width="32"
                        Height="32"
                        CornerRadius="10"
                        Background="{StaticResource SettingsAccentGradientBrush}"
                        VerticalAlignment="Center">
                    <FontIcon FontFamily="Segoe MDL2 Assets"
                            Glyph="&#xE8A7;"
                            FontSize="16"
                            Foreground="White"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
                </Border>
                <StackPanel Spacing="0"
                        VerticalAlignment="Center">
                    <TextBlock Text="Dev Grid Settings"
                            FontSize="14"
                            FontWeight="SemiBold"/>
                    <TextBlock Text="Workspaces, templates, and launch behavior"
                            FontSize="11"
                            Opacity="0.62"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Main two-column layout -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="LeftPaneColumn"
                        Width="292"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left pane: clear navigation sections -->
            <Border Grid.Column="0"
                    Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    Padding="18,18,16,18"
                    CornerRadius="18"
                    Margin="16,10,0,18">
                <Grid RowSpacing="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0"
                                Spacing="4"
                                Margin="2,0,2,8">
                        <TextBlock Text="SETTINGS"
                                  Style="{StaticResource SettingsSectionLabelStyle}"/>
                        <TextBlock Text="Keep the surface focused: only workspace behavior and templates live here."
                                  Style="{StaticResource SettingsPageSubtitleStyle}"/>
                    </StackPanel>

                    <!-- Fixed General item -->
                    <ListViewItem Grid.Row="1"
                                  x:Name="GeneralItem"
                                  IsSelected="{x:Bind ViewModel.IsGeneralSelected, Mode=TwoWay}"
                                  Tapped="OnGeneralItemTapped"
                                  Style="{StaticResource ToolbarListItemStyle}"
                                  Padding="4,6">
                        <Grid ColumnSpacing="10" Padding="6,2,4,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="28"
                                    Height="28"
                                    CornerRadius="8"
                                    Background="{ThemeResource SubtleFillColorSecondaryBrush}">
                                <FontIcon FontFamily="Segoe MDL2 Assets"
                                         Glyph="&#xE713;"
                                         FontSize="14"
                                         VerticalAlignment="Center"
                                         HorizontalAlignment="Center"/>
                            </Border>
                            <TextBlock Grid.Column="1"
                                       Text="General"
                                       FontWeight="Normal"
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </ListViewItem>

                    <ListViewItem Grid.Row="2"
                                  x:Name="TemplatesItem"
                                  IsSelected="{x:Bind ViewModel.IsTemplatesSelected, Mode=TwoWay}"
                                  Tapped="OnTemplatesItemTapped"
                                  Style="{StaticResource ToolbarListItemStyle}"
                                  Padding="4,6">
                        <Grid ColumnSpacing="10" Padding="6,2,4,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="28"
                                    Height="28"
                                    CornerRadius="8"
                                    Background="{ThemeResource SubtleFillColorSecondaryBrush}">
                                <FontIcon FontFamily="Segoe MDL2 Assets"
                                         Glyph="&#xE8D2;"
                                         FontSize="14"
                                         VerticalAlignment="Center"
                                         HorizontalAlignment="Center"/>
                            </Border>
                            <TextBlock Grid.Column="1"
                                       Text="Templates"
                                       FontWeight="Normal"
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </ListViewItem>

                    <!-- Groups header with add/remove -->
                    <Grid Grid.Row="3"
                          Margin="0,8,0,2"
                          ColumnSpacing="8"
                          Visibility="Collapsed">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center"
                                    Spacing="8">
                            <FontIcon FontFamily="Segoe MDL2 Assets"
                                      Glyph="&#xE8FD;"
                                      FontSize="13"
                                      VerticalAlignment="Center"/>
                            <TextBlock Text="Groups"
                                    FontSize="15"
                                    FontWeight="SemiBold"
                                    VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal"
                                    Grid.Column="1"
                                    Spacing="6">
                            <Button Style="{StaticResource AccentButtonStyle}"
                                    MinWidth="0"
                                    Padding="10,4"
                                    Content="Add"
                                    Click="OnAddGroup"/>
                            <Button MinWidth="0"
                                    Padding="10,4"
                                    Content="Remove"
                                    Click="OnRemoveSelectedGroup"
                                    IsEnabled="{x:Bind ViewModel.CanRemoveSelectedGroup, Mode=OneWay}"/>
                        </StackPanel>
                    </Grid>
                    <ListView Grid.Row="4"
                              x:Name="GroupsList"
                              ItemsSource="{x:Bind ViewModel.Groups, Mode=OneWay}"
                              SelectedItem="{x:Bind ViewModel.SelectedGroup, Mode=TwoWay}"
                              SelectionMode="Single"
                              BorderThickness="0"
                              IsItemClickEnabled="True"
                              Visibility="Collapsed"
                              MinHeight="120"
                              ItemContainerStyle="{StaticResource ToolbarListItemStyle}">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:ButtonGroup">
                                <Grid ColumnSpacing="8"
                                        Padding="6,0,4,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="NameText"
                                            Text="{x:Bind Name, Mode=OneWay}"
                                            FontWeight="Normal"
                                            VerticalAlignment="Center"
                                            TextTrimming="CharacterEllipsis"/>
                                    <TextBox x:Name="NameEdit"
                                            Text="{x:Bind Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            Visibility="Collapsed"
                                            VerticalAlignment="Center"
                                            MinWidth="80"
                                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                            KeyDown="OnGroupNameTextBoxKeyDown"
                                            LostFocus="OnGroupNameTextBoxLostFocus"/>
                                    <Button Grid.Column="1"
                                            x:Name="RenameButton"
                                            Click="OnStartRenameGroup"
                                            Tag="{x:Bind}"
                                            Style="{StaticResource ButtonStyleSmallIcon}"
                                            ToolTipService.ToolTip="{Binding Id, Converter={StaticResource GroupIdToRenameUiConverter}, ConverterParameter=tooltip}"
                                            IsEnabled="{Binding Id, Converter={StaticResource GroupIdToRenameUiConverter}, ConverterParameter=enabled}"
                                            Width="28"
                                            Height="28"
                                            Padding="0"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Center">
                                        <FontIcon FontFamily="Segoe MDL2 Assets"
                                                Glyph="{Binding Id, Converter={StaticResource GroupIdToRenameUiConverter}, ConverterParameter=glyph}"
                                                FontSize="14"/>
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.Resources>
                            <Style TargetType="ListViewItem"
                                    BasedOn="{StaticResource ToolbarListItemStyle}">
                                <Setter Property="Padding"
                                        Value="4,6"/>
                            </Style>
                        </ListView.Resources>
                    </ListView>

                    <!-- Workspaces header/list are hidden in template-only settings mode -->
                    <Grid Grid.Row="5"
                          Margin="0,8,0,2"
                          ColumnSpacing="8"
                          Visibility="Collapsed">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center"
                                    Spacing="8">
                            <FontIcon FontFamily="Segoe MDL2 Assets"
                                      Glyph="&#xE7F4;"
                                      FontSize="13"
                                      VerticalAlignment="Center"/>
                            <StackPanel Spacing="0"
                                        VerticalAlignment="Center">
                                <TextBlock Text="Workspaces"
                                        FontSize="15"
                                        FontWeight="SemiBold"/>
                                <TextBlock Text="Saved layouts"
                                        Opacity="0.7"/>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal"
                                    Grid.Column="1"
                                    Spacing="6">
                            <Button MinWidth="0"
                                    Padding="10,4"
                                    Content="Remove"
                                    Click="OnRemoveWorkspace"
                                    IsEnabled="{x:Bind ViewModel.HasSelectedWorkspace, Mode=OneWay}"/>
                        </StackPanel>
                    </Grid>
                    <ListView Grid.Row="6"
                              x:Name="WorkspacesList"
                              ItemsSource="{x:Bind ViewModel.WorkspaceButtons, Mode=OneWay}"
                              SelectedItem="{x:Bind ViewModel.SelectedWorkspace, Mode=TwoWay}"
                              SelectionMode="Single"
                              BorderThickness="0"
                              IsItemClickEnabled="True"
                              MinHeight="120"
                              Visibility="Collapsed"
                              ItemContainerStyle="{StaticResource ToolbarListItemStyle}">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="vm:WorkspaceButtonViewModel">
                                <Grid ColumnSpacing="8"
                                        Padding="6,0,4,0"
                                        Opacity="{x:Bind DisplayOpacity}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <FontIcon FontFamily="Segoe MDL2 Assets"
                                            Glyph="{x:Bind IconGlyph}"
                                            FontSize="14"
                                            VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1"
                                            Text="{x:Bind DisplayTitle, Mode=OneWay}"
                                            FontWeight="Normal"
                                            VerticalAlignment="Center"
                                            TextTrimming="CharacterEllipsis"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.Resources>
                            <Style TargetType="ListViewItem"
                                    BasedOn="{StaticResource ToolbarListItemStyle}">
                                <Setter Property="Padding"
                                        Value="4,6"/>
                            </Style>
                        </ListView.Resources>
                    </ListView>
                </Grid>
            </Border>

            <!-- Divider line intentionally omitted; cards provide separation. -->

            <!-- Right pane: buttons + button details -->
            <ScrollViewer Grid.Column="2"
                    Padding="0"
                    VerticalScrollBarVisibility="Auto">
                <Border Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="22"
                        Padding="34,30,38,48"
                        Margin="0,10,18,18">
                    <StackPanel Spacing="28">
                        <!-- General settings (only when General is selected) -->
                        <StackPanel Spacing="18" Visibility="{x:Bind ViewModel.IsGeneralSelected, Mode=OneWay}">
                            <Grid ColumnSpacing="16"
                                  Margin="0,0,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="48"
                                        Height="48"
                                        CornerRadius="14"
                                        Background="{StaticResource SettingsAccentGradientBrush}"
                                        VerticalAlignment="Top">
                                    <FontIcon FontFamily="Segoe MDL2 Assets"
                                              Glyph="&#xE713;"
                                              Foreground="White"
                                              FontSize="21"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel Grid.Column="1"
                                            Spacing="5"
                                            VerticalAlignment="Center">
                                    <TextBlock Text="General"
                                               Style="{StaticResource SettingsPageTitleStyle}"/>
                                    <TextBlock Text="Choose how Dev Grid appears, how it is invoked, and which visual theme it uses."
                                               Style="{StaticResource SettingsPageSubtitleStyle}"/>
                                </StackPanel>
                            </Grid>

                            <tkcontrols:SettingsCard x:Name="StartupSettingsCard"
                                                    Description="{x:Bind ViewModel.StartupStatusText, Mode=OneWay}">
                                <tkcontrols:SettingsCard.Header>
                                    <StackPanel Orientation="Horizontal" Spacing="10">
                                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE7C3;" FontSize="15"/>
                                        <TextBlock Text="Run at startup" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </tkcontrols:SettingsCard.Header>
                                <ToggleSwitch x:Name="StartupToggle"
                                              IsOn="{x:Bind ViewModel.IsStartupEnabled, Mode=OneWay}"
                                              IsEnabled="{x:Bind ViewModel.IsStartupAvailable, Mode=OneWay}"
                                              Toggled="OnStartupToggled"/>
                            </tkcontrols:SettingsCard>

                            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1"
                                    CornerRadius="12"
                                    Padding="16,12">
                                <StackPanel Spacing="10"
                                            HorizontalAlignment="Stretch">
                                    <StackPanel Spacing="2">
                                        <StackPanel Orientation="Horizontal" Spacing="10">
                                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE7F4;" FontSize="15"/>
                                            <TextBlock Text="Invocation"
                                                       FontSize="13"
                                                       FontWeight="SemiBold"/>
                                        </StackPanel>
                                        <TextBlock Text="Choose how Dev Grid appears when invoked."
                                                   Opacity="0.72"/>
                                    </StackPanel>

                                    <Border Height="1"
                                            Background="{ThemeResource CardStrokeColorDefaultBrush}"
                                            Opacity="0.35"
                                            Margin="0,2,0,2"/>

                                    <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                            BorderThickness="1"
                                            CornerRadius="12"
                                            Padding="2">
                                        <StackPanel Spacing="0">
                                            <tkcontrols:SettingsCard ContentAlignment="Right">
                                                <tkcontrols:SettingsCard.Header>
                                                    <StackPanel Orientation="Horizontal" Spacing="10">
                                                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8A7;" FontSize="14"/>
                                                        <TextBlock Text="Mode"/>
                                                    </StackPanel>
                                                </tkcontrols:SettingsCard.Header>
                                                <ComboBox Width="260"
                                                          SelectedIndex="{x:Bind ViewModel.DisplayModeIndex, Mode=TwoWay}"
                                                          HorizontalAlignment="Right">
                                                    <ComboBoxItem Content="Top bar (screen edge trigger)"/>
                                                    <ComboBoxItem Content="Radial menu (Ctrl+Shift+Space)"/>
                                                </ComboBox>
                                            </tkcontrols:SettingsCard>

                                            <tkcontrols:SettingsCard Description="Only trigger when Ctrl is held in Top bar mode."
                                                                    ContentAlignment="Right">
                                                <tkcontrols:SettingsCard.Header>
                                                    <StackPanel Orientation="Horizontal" Spacing="10">
                                                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE765;" FontSize="14"/>
                                                        <TextBlock Text="Require Ctrl"/>
                                                    </StackPanel>
                                                </tkcontrols:SettingsCard.Header>
                                                <ToggleSwitch HorizontalAlignment="Right"
                                                              IsOn="{x:Bind ViewModel.RequireCtrlForTopBarTrigger, Mode=TwoWay}"
                                                              IsEnabled="{x:Bind ViewModel.IsTopBarModeSelected, Mode=OneWay}"
                                                              OnContent=""
                                                              OffContent=""/>
                                            </tkcontrols:SettingsCard>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Border>

                            <tkcontrols:SettingsCard Description="Choose a built-in toolbar visual style.">
                                <tkcontrols:SettingsCard.Header>
                                    <StackPanel Orientation="Horizontal" Spacing="10">
                                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE790;" FontSize="15"/>
                                        <TextBlock Text="Theme" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </tkcontrols:SettingsCard.Header>
                                <ComboBox SelectedIndex="{x:Bind ViewModel.ThemeIndex, Mode=TwoWay}">
                                    <ComboBoxItem Content="Sand Mist"/>
                                    <ComboBoxItem Content="Glacier Blue"/>
                                    <ComboBoxItem Content="Apricot Dawn"/>
                                    <ComboBoxItem Content="Cobalt Coral"/>
                                    <ComboBoxItem Content="Teal Amber"/>
                                    <ComboBoxItem Content="Slate Sky"/>
                                    <ComboBoxItem Content="Graphite Azure"/>
                                    <ComboBoxItem Content="Burgundy Gold"/>
                                    <ComboBoxItem Content="Neon Tide"/>
                                    <ComboBoxItem Content="Deep Teal Gold"/>
                                    <ComboBoxItem Content="Cloud Monochrome"/>
                                </ComboBox>
                            </tkcontrols:SettingsCard>

                        </StackPanel>

                        <StackPanel Spacing="18" Visibility="{x:Bind ViewModel.IsTemplatesSelected, Mode=OneWay}">
                            <Grid ColumnSpacing="16"
                                  Margin="0,0,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="48"
                                        Height="48"
                                        CornerRadius="14"
                                        Background="{StaticResource SettingsAccentGradientBrush}"
                                        VerticalAlignment="Top">
                                    <FontIcon FontFamily="Segoe MDL2 Assets"
                                              Glyph="&#xE8D2;"
                                              Foreground="White"
                                              FontSize="21"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel Grid.Column="1"
                                            Spacing="5"
                                            VerticalAlignment="Center">
                                    <TextBlock Text="Templates"
                                               Style="{StaticResource SettingsPageTitleStyle}"/>
                                    <TextBlock Text="Design reusable workspace blueprints. Snapshot stays outside Settings for quick ad-hoc capture."
                                               Style="{StaticResource SettingsPageSubtitleStyle}"/>
                                </StackPanel>
                            </Grid>

                            <StackPanel Orientation="Horizontal"
                                        Spacing="10">
                                <Button Content="Create template"
                                        Style="{StaticResource AccentButtonStyle}"
                                        HorizontalAlignment="Left"
                                        Click="OnCreateWorkspaceTemplate"/>
                                <Button x:Name="DeleteSelectedTemplateButton"
                                        Content="Delete"
                                        Style="{StaticResource SettingsPillButtonStyle}"
                                        HorizontalAlignment="Left"
                                        IsEnabled="False"
                                        Click="OnDeleteSelectedTemplate"/>
                            </StackPanel>

                            <Grid ColumnSpacing="16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="300"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0"
                                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1"
                                        CornerRadius="16"
                                        Padding="14">
                                    <StackPanel Spacing="10">
                                        <StackPanel Spacing="3">
                                            <StackPanel Orientation="Horizontal" Spacing="9">
                                                <FontIcon FontFamily="Segoe MDL2 Assets"
                                                          Glyph="&#xE8F1;"
                                                          FontSize="15"/>
                                                <TextBlock Text="Template library"
                                                           FontSize="14"
                                                           FontWeight="SemiBold"/>
                                            </StackPanel>
                                            <TextBlock Text="Workspace templates"
                                                       Opacity="0.68"/>
                                        </StackPanel>
                                        <ListView x:Name="WorkspaceTemplatesList"
                                                  SelectionMode="Single"
                                                  MinHeight="410"
                                                  MaxHeight="520"
                                                  SelectionChanged="OnWorkspaceTemplatesListSelectionChanged"
                                                  Margin="0,2,0,0">
                                            <ListView.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Spacing="2" Padding="4,4,4,4">
                                                        <TextBlock Text="{Binding DisplayName}"
                                                                   FontWeight="SemiBold"
                                                                   TextTrimming="CharacterEllipsis"/>
                                                        <TextBlock Text="{Binding NameAndKind}"
                                                                   Opacity="0.72"
                                                                   TextTrimming="CharacterEllipsis"/>
                                                        <TextBlock Text="{Binding Description}"
                                                                   Opacity="0.62"
                                                                   TextWrapping="WrapWholeWords"
                                                                   MaxLines="2"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ListView.ItemTemplate>
                                        </ListView>

                                        <TextBlock Text="Agent templates"
                                                   Opacity="0.72"
                                                   Margin="0,8,0,0"
                                                   Visibility="Collapsed"/>
                                        <ListView x:Name="AgentTemplatesList"
                                                  SelectionMode="Single"
                                                  MinHeight="170"
                                                  MaxHeight="200"
                                                  SelectionChanged="OnAgentTemplatesListSelectionChanged"
                                                  Margin="0,2,0,0"
                                                  Visibility="Collapsed">
                                            <ListView.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Spacing="2" Padding="4,4,4,4">
                                                        <TextBlock Text="{Binding DisplayName}"
                                                                   FontWeight="SemiBold"
                                                                   TextTrimming="CharacterEllipsis"/>
                                                        <TextBlock Text="{Binding NameAndKind}"
                                                                   Opacity="0.72"
                                                                   TextTrimming="CharacterEllipsis"/>
                                                        <TextBlock Text="{Binding Description}"
                                                                   Opacity="0.62"
                                                                   TextWrapping="WrapWholeWords"
                                                                   MaxLines="2"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ListView.ItemTemplate>
                                        </ListView>

                                        <TextBlock x:Name="TemplatesEmptyText"
                                                   Text="No templates yet. Create one to get started."
                                                   Opacity="0.7"
                                                   Visibility="Collapsed"/>
                                    </StackPanel>
                                </Border>

                                <Border x:Name="TemplateEditorBorder"
                                        Grid.Column="1"
                                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1"
                                        CornerRadius="16"
                                        Padding="18">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                                  HorizontalScrollBarVisibility="Disabled"
                                                  MaxHeight="620">
                                        <StackPanel Spacing="14">
                                            <StackPanel Orientation="Horizontal" Spacing="9">
                                                <FontIcon FontFamily="Segoe MDL2 Assets"
                                                          Glyph="&#xE70F;"
                                                          FontSize="15"/>
                                                <TextBlock Text="Template configuration"
                                                           FontSize="14"
                                                           FontWeight="SemiBold"/>
                                            </StackPanel>
                                            <TextBlock x:Name="TemplateEditorHintText"
                                                       Text="Select a template from the left list."
                                                       Opacity="0.72"
                                                       TextWrapping="Wrap"/>

                                            <TextBlock Text="Template id"
                                                       Opacity="0.72"/>
                                            <TextBox x:Name="TemplateIdTextBox"
                                                     IsReadOnly="True"/>

                                            <TextBlock Text="Display name"
                                                       Opacity="0.72"/>
                                            <TextBox x:Name="TemplateDisplayNameTextBox"/>

                                            <TextBlock Text="Description"
                                                       Opacity="0.72"/>
                                            <TextBox x:Name="TemplateDescriptionTextBox"
                                                     AcceptsReturn="True"
                                                     TextWrapping="Wrap"
                                                     MinHeight="64"/>

                                            <TextBlock Text="Template type"
                                                       Opacity="0.72"
                                                       Visibility="Collapsed"/>
                                            <ComboBox x:Name="TemplateKindComboBox"
                                                      SelectionChanged="OnTemplateKindSelectionChanged"
                                                      Visibility="Collapsed">
                                                <ComboBoxItem Content="workspace"/>
                                                <ComboBoxItem Content="agent"/>
                                            </ComboBox>

                                            <Grid x:Name="TemplateWorkspaceLayoutSection"
                                                  ColumnSpacing="10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" Spacing="6">
                                                    <TextBlock Text="Window layout"
                                                               Opacity="0.72"/>
                                                    <TextBlock Text="Default tiling layout (auto based on enabled apps)."
                                                               Opacity="0.62"
                                                               TextWrapping="Wrap"/>
                                                    <ComboBox x:Name="TemplateLayoutStrategyComboBox"
                                                              Visibility="Collapsed"
                                                               IsEnabled="False"
                                                               SelectedIndex="1">
                                                        <ComboBoxItem Content="single"/>
                                                        <ComboBoxItem Content="side-by-side"/>
                                                        <ComboBoxItem Content="triple-columns"/>
                                                        <ComboBoxItem Content="grid-2x2"/>
                                                    </ComboBox>
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Spacing="6">
                                                    <TextBlock Text="Monitor policy"
                                                               Opacity="0.72"/>
                                                    <ComboBox x:Name="TemplateMonitorPolicyComboBox"
                                                              SelectedIndex="0">
                                                        <ComboBoxItem Content="primary"/>
                                                        <ComboBoxItem Content="current"/>
                                                        <ComboBoxItem Content="any"/>
                                                    </ComboBox>
                                                </StackPanel>
                                            </Grid>

                                            <ToggleSwitch x:Name="TemplateRequiresRepoToggle"
                                                          Header="Requires repository path"
                                                          IsOn="True"
                                                          OnContent=""
                                                          OffContent=""/>
                                            <TextBox x:Name="TemplateDefaultRepoTextBox"
                                                     PlaceholderText="Default repo path (optional)"/>

                                            <ToggleSwitch x:Name="TemplateCreateWorktreeToggle"
                                                          Header="Create git worktree by default"
                                                          IsOn="False"
                                                          OnContent=""
                                                          OffContent=""/>
                                            <TextBox x:Name="TemplateWorktreeBaseBranchTextBox"
                                                     PlaceholderText="Base branch (default: main)"
                                                     Text="main"/>

                                            <Border x:Name="TemplateAgentSectionDivider"
                                                    Height="1"
                                                    Background="{ThemeResource CardStrokeColorDefaultBrush}"
                                                    Opacity="0.35"/>

                                            <StackPanel x:Name="TemplateAgentSection"
                                                        Spacing="8"
                                                        Visibility="Collapsed">
                                                <TextBlock Text="Agent startup"
                                                           FontWeight="SemiBold"/>
                                                <TextBlock Text="Command to run in a new Windows Terminal window."
                                                           Opacity="0.62"/>
                                                <ToggleSwitch x:Name="TemplateEnableAgentToggle"
                                                              Header="Enable agent command"
                                                              Visibility="Collapsed"
                                                              IsOn="False"
                                                              OnContent=""
                                                              OffContent=""/>
                                                <ComboBox x:Name="TemplateAgentComboBox"
                                                          Visibility="Collapsed"
                                                          SelectedIndex="0">
                                                    <ComboBoxItem Content="copilot"/>
                                                </ComboBox>
                                                <TextBox x:Name="TemplateAgentCommandTextBox"
                                                         PlaceholderText="Agent command (for example: copilot)"/>
                                            </StackPanel>

                                            <Border x:Name="TemplateWorkspaceAppsDivider"
                                                    Height="1"
                                                    Background="{ThemeResource CardStrokeColorDefaultBrush}"
                                                    Opacity="0.35"/>

                                            <StackPanel x:Name="TemplateWorkspaceApplicationsSection"
                                                        Spacing="8">
                                                <TextBlock Text="Applications"
                                                           FontWeight="SemiBold"/>
                                                <TextBlock Text="Only enabled apps below are included in layout calculation."
                                                           Opacity="0.62"/>
                                                <ToggleSwitch x:Name="TemplateIncludeTerminalToggle"
                                                              Header="Windows Terminal"
                                                              IsOn="True"
                                                              OnContent=""
                                                              OffContent=""/>
                                                <ToggleSwitch x:Name="TemplateIncludeVsCodeToggle"
                                                              Header="VS Code"
                                                              IsOn="True"
                                                              OnContent=""
                                                              OffContent=""/>
                                                <ToggleSwitch x:Name="TemplateIncludeVisualStudioToggle"
                                                              Header="Visual Studio (devenv)"
                                                              IsOn="False"
                                                              OnContent=""
                                                              OffContent=""/>
                                                <TextBox x:Name="TemplateVisualStudioSolutionTextBox"
                                                         PlaceholderText="Optional .sln path relative to repo"/>
                                                <ToggleSwitch x:Name="TemplateIncludeLogsToggle"
                                                              Header="Logs folder"
                                                              IsOn="False"
                                                              OnContent=""
                                                              OffContent=""/>
                                                <TextBox x:Name="TemplateLogsPathTextBox"
                                                         PlaceholderText="Folder path (default: {repo})"/>
                                            </StackPanel>

                                            <TextBlock x:Name="TemplateEditorStatusText"
                                                       Opacity="0.72"
                                                       TextWrapping="Wrap"/>

                                            <Button x:Name="SaveSelectedTemplateButton"
                                                    Content="Save template"
                                                    Style="{StaticResource AccentButtonStyle}"
                                                    HorizontalAlignment="Left"
                                                    IsEnabled="False"
                                                    Click="OnSaveSelectedTemplate"/>
                                        </StackPanel>
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </StackPanel>

                        <!-- Editable group settings (user-created groups only) -->
                        <StackPanel Spacing="16" Visibility="Collapsed">
                            <tkcontrols:SettingsCard x:Name="GroupNameEnableButton" 
                                                            Header="{x:Bind ViewModel.SelectedGroup.Name, Mode=OneWay}"
                                                            Description="{x:Bind ViewModel.SelectedGroup.Description, Mode=OneWay}">
                                <ToggleSwitch IsOn="{x:Bind ViewModel.SelectedGroup.IsEnabled, Mode=TwoWay}" />
                            </tkcontrols:SettingsCard>

                            <!-- Buttons header with add/remove -->
                            <Grid ColumnSpacing="10"
                                    VerticalAlignment="Center"
                                    Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Buttons"
                                    FontSize="15"
                                    FontWeight="SemiBold"
                                    VerticalAlignment="Center"
                                    Grid.Column="0"/>
                                <Rectangle Width="1"
                                    Height="18"
                                    Fill="{ThemeResource CardStrokeColorDefaultBrush}"
                                    Opacity="0.35"
                                    VerticalAlignment="Center"
                                    Grid.Column="1"/>
                                <StackPanel Orientation="Horizontal"
                                    Spacing="6"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Grid.Column="2">
                                    <Button Content="Add"
                                        Click="OnAddButton"
                                        Tag="{x:Bind ViewModel.SelectedGroup}"
                                        IsEnabled="{x:Bind ViewModel.CanAddButtonToSelectedGroup, Mode=OneWay}"
                                        VerticalAlignment="Center"/>
                                </StackPanel>
                            </Grid>

                            <!-- Buttons as expandable settings -->
                            <ItemsControl ItemsSource="{x:Bind ViewModel.SelectedGroup.Buttons, Mode=OneWay}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="local:ToolbarButton">
                                        <tkcontrols:SettingsExpander IsExpanded="{x:Bind IsExpanded, Mode=TwoWay}"
                                                                 Header="{x:Bind DisplayName, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="12">
                                                <ToggleSwitch IsOn="{x:Bind IsEnabled, Mode=TwoWay}" OnContent="" OffContent=""/>
                                                <Button Click="OnRemoveButton"
                                                    ToolTipService.ToolTip="Remove"
                                                    Background="Transparent"
                                                    BorderBrush="Transparent">
                                                    <FontIcon Glyph="&#xE74D;" FontSize="16" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                                                </Button>
                                            </StackPanel>
                                            <tkcontrols:SettingsExpander.Items>
                                                <tkcontrols:SettingsCard Header="Name">
                                                    <TextBox Text="{x:Bind Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                        Style="{StaticResource StandardTextBoxStyle}"/>
                                                </tkcontrols:SettingsCard>
                                                <tkcontrols:SettingsCard Header="Command">
                                                    <TextBox Text="{x:Bind Action.Command, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                        Style="{StaticResource StandardTextBoxStyle}"/>
                                                </tkcontrols:SettingsCard>
                                                <tkcontrols:SettingsCard Header="Arguments">
                                                    <TextBox Text="{x:Bind Action.Arguments, Mode=TwoWay}"
                                                        Style="{StaticResource StandardTextBoxStyle}"/>
                                                </tkcontrols:SettingsCard>
                                                <tkcontrols:SettingsCard Header="Customize Icon"
                                                                     Description="Override the auto-detected icon">
                                                    <ToggleSwitch IsOn="{x:Bind IsIconCustomized, Mode=TwoWay}" OnContent="" OffContent=""/>
                                                </tkcontrols:SettingsCard>
                                                <tkcontrols:SettingsCard Header="Icon Type"
                                                                     Visibility="{x:Bind IsIconCustomized, Mode=OneWay}">
                                                    <ComboBox SelectedIndex="{x:Bind IconTypeIndex, Mode=TwoWay}">
                                                        <ComboBoxItem Content="Icon"/>
                                                        <ComboBoxItem Content="Image"/>
                                                    </ComboBox>
                                                </tkcontrols:SettingsCard>
                                                <tkcontrols:SettingsCard Header="Icon"
                                                                     Visibility="{x:Bind IsIconCustomized, Mode=OneWay}">
                                                    <Button Content="Choose from library"
                                                        Click="OnChooseIconFromLibrary"
                                                        Tag="{x:Bind}" />
                                                </tkcontrols:SettingsCard>
                                                <tkcontrols:SettingsCard Header="Image Path"
                                                                     Visibility="{x:Bind IsIconCustomized, Mode=OneWay}">
                                                    <Grid ColumnSpacing="8">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBox Grid.Column="0"
                                                            Text="{x:Bind IconPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                            Style="{StaticResource StandardTextBoxStyle}"/>
                                                        <Button Grid.Column="1"
                                                            Content="Browse"
                                                            Click="OnBrowseIcon"
                                                            Tag="{x:Bind}"/>
                                                    </Grid>
                                                </tkcontrols:SettingsCard>
                                            </tkcontrols:SettingsExpander.Items>
                                        </tkcontrols:SettingsExpander>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                        <!-- Default groups (locked) -->
                        <StackPanel Spacing="16" Visibility="Collapsed">
                            <tkcontrols:SettingsCard Header="Legacy Actions"
                                                    Description="Dev Grid mode disables legacy media/system controls.">
                                <TextBlock Text="This section is kept only for backward compatibility."
                                           TextWrapping="Wrap"/>
                            </tkcontrols:SettingsCard>
                        </StackPanel>

                        <!-- Workspace settings are hidden in template-only settings mode -->
                        <StackPanel Spacing="16" Visibility="Collapsed">
                            <TextBlock Text="Workspace"
                                       FontSize="18"
                                       FontWeight="SemiBold"/>

                            <tkcontrols:SettingsCard Header="Name">
                                <TextBox Text="{x:Bind ViewModel.SelectedWorkspace.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        Style="{StaticResource StandardTextBoxStyle}"/>
                            </tkcontrols:SettingsCard>

                            <tkcontrols:SettingsCard Header="Description">
                                <TextBox Text="{x:Bind ViewModel.SelectedWorkspace.Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        Style="{StaticResource StandardTextBoxStyle}"/>
                            </tkcontrols:SettingsCard>

                            <tkcontrols:SettingsCard Header="Enabled">
                                <ToggleSwitch IsOn="{x:Bind ViewModel.SelectedWorkspace.Enabled, Mode=TwoWay}"/>
                            </tkcontrols:SettingsCard>

                            <tkcontrols:SettingsCard Header="Workspace title">
                                <TextBlock Text="{x:Bind ViewModel.SelectedWorkspace.WorkspaceTitle, Mode=OneWay}"
                                           TextWrapping="Wrap"/>
                            </tkcontrols:SettingsCard>

                            <tkcontrols:SettingsCard Header="Template">
                                <TextBlock Text="{x:Bind ViewModel.SelectedWorkspace.TemplateName, Mode=OneWay}"
                                           TextWrapping="Wrap"/>
                            </tkcontrols:SettingsCard>

                            <tkcontrols:SettingsCard Header="Instance">
                                <TextBlock Text="{x:Bind ViewModel.SelectedWorkspace.InstanceName, Mode=OneWay}"
                                           TextWrapping="Wrap"/>
                            </tkcontrols:SettingsCard>

                            <tkcontrols:SettingsCard Header="Repository root">
                                <TextBlock Text="{x:Bind ViewModel.SelectedWorkspace.RepoRoot, Mode=OneWay}"
                                           TextWrapping="Wrap"/>
                            </tkcontrols:SettingsCard>

                            <tkcontrols:SettingsCard Header="Focus priority">
                                <TextBlock Text="{x:Bind ViewModel.SelectedWorkspace.FocusPriorityDisplay, Mode=OneWay}"
                                           TextWrapping="Wrap"/>
                            </tkcontrols:SettingsCard>

                            <tkcontrols:SettingsCard Header="Last launched">
                                <TextBlock Text="{x:Bind ViewModel.SelectedWorkspace.LastLaunchedDisplay, Mode=OneWay}"
                                           TextWrapping="Wrap"/>
                            </tkcontrols:SettingsCard>

                            <!-- Applications header with add -->
                            <Grid ColumnSpacing="10"
                                    VerticalAlignment="Center"
                                    Margin="0,6,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Applications"
                                        FontSize="15"
                                        FontWeight="SemiBold"
                                        VerticalAlignment="Center"
                                        Grid.Column="0"/>
                                <Rectangle Width="1"
                                        Height="18"
                                        Fill="{ThemeResource CardStrokeColorDefaultBrush}"
                                        Opacity="0.35"
                                        VerticalAlignment="Center"
                                        Grid.Column="1"/>
                                <StackPanel Orientation="Horizontal"
                                        Spacing="6"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Grid.Column="2">
                                    <Button Content="Add app"
                                            Click="OnAddWorkspaceApp"
                                            VerticalAlignment="Center"/>
                                </StackPanel>
                            </Grid>

                            <TextBlock Text="No applications yet."
                                       Opacity="0.65"
                                       Visibility="{x:Bind ViewModel.SelectedWorkspace.IsEmpty, Mode=OneWay}"/>

                            <ItemsControl ItemsSource="{x:Bind ViewModel.SelectedWorkspace.Apps, Mode=OneWay}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="workspaces:ApplicationDefinition">
                                        <tkcontrols:SettingsExpander IsExpanded="{x:Bind IsExpanded, Mode=TwoWay}"
                                                                     Header="{x:Bind DisplayName, Mode=OneWay}"
                                                                     Description="{x:Bind Path, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="12">
                                                <Button Click="OnRemoveWorkspaceApp"
                                                        ToolTipService.ToolTip="Remove"
                                                        Background="Transparent"
                                                        BorderBrush="Transparent">
                                                    <FontIcon Glyph="&#xE74D;" FontSize="16" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                                                </Button>
                                            </StackPanel>
                                            <tkcontrols:SettingsExpander.Items>
                                                <tkcontrols:SettingsCard Header="Name">
                                                    <TextBox Text="{x:Bind Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                            Style="{StaticResource StandardTextBoxStyle}"/>
                                                </tkcontrols:SettingsCard>
                                                <tkcontrols:SettingsCard Header="Path">
                                                    <TextBox Text="{x:Bind Path, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                            Style="{StaticResource StandardTextBoxStyle}"/>
                                                </tkcontrols:SettingsCard>
                                                <tkcontrols:SettingsCard Header="Command line">
                                                    <TextBox Text="{x:Bind CommandLineArguments, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                            Style="{StaticResource StandardTextBoxStyle}"/>
                                                </tkcontrols:SettingsCard>
                                                <tkcontrols:SettingsCard Header="Working directory">
                                                    <TextBox Text="{x:Bind WorkingDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                            Style="{StaticResource StandardTextBoxStyle}"/>
                                                </tkcontrols:SettingsCard>
                                            </tkcontrols:SettingsExpander.Items>
                                        </tkcontrols:SettingsExpander>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </ScrollViewer>
        </Grid>
    </Grid>
</winuiex:WindowEx>
